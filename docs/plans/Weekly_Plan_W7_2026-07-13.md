> 상위 문서: `KTX_Ticketing_Development_Plan.md` (★확정 일정 W7·M5)·`KTX_Ticketing_Task_Checklist.md` (P6)
> 출처: 2026-07-13 세션 — W7 주간 계획 수립
> 상태: 확정 (주 15h · P6 산출물 완결 → M5 제출 범위)
> 연관: T6-1~T6-7(P6)·`Portfolio_Project_Evaluation_Criteria.md`(셀프체크 7항목)·`P4_Result.md`(그래프 소스)·B-4(시드 출발일 블로커)

# 주간 계획 — W7 (2026-07-13 월 ~ 07-19 일)

## 0. 한 줄 요약

이번 주 = **P6 산출물을 완성해 M5(제출 가능) 달성 — 마지막 스프린트**.
README(문제정의·아키텍처·트레이드오프·성능 그래프) + 다이어그램 이미지화 + 동작 영상(정상·동시성/부하)을 확보하고 셀프 체크리스트 7항목을 통과시킨다.
배포는 **영상 우선·URL은 시도만**(개발계획 방침: 차단 시 영상 보완). 앞 페이즈(P4)의 수치·그래프가 이미 확보돼 있으므로 이번 주는 **산출물 조립**이 핵심이지 신규 측정이 아니다.

> **⚠️ 선결 블로커 B-4 (시드 출발일 하드코딩)**: `DataInitializer` 가 `departure_time = 2026-07-01 + i일` 하드코딩이라 오늘(07/13) 기준 앞쪽 스케줄 다수가 이미 출발 → 예매 경로 시연/부하가 거짓 실패(SoldOut·reserve_ok=0). **영상(T6-4/5)과 배포 시연이 예매 경로를 태우므로 B-4를 먼저 닫아야 한다.** 근본 수정 = 출발일을 `now()` 상대날짜화 + `config.js` FROM_DATE·L3b 50일 매핑 동반 수정. (백로그 B-4 정본 참조)

## 1. 진입 좌표 (2026-07-13 기준)

- 전체 진행률 65% (35/54). P0~P4 핵심(M1·M2·M3·M4) 완료 — **수치·Before/After 그래프 확보 완료**(`P4_Result.md`).
- **P6 산출물 0%**(0/7) — 이번 주 집중 대상. P5(MQ 비동기)는 Could 로 범위 밖.
- 원 일정상 W7(07/13~19) = P6 산출물 전념 → **M5 제출(07/19 일)**.
- 가용 시간 = **주 15h 안팎**. 6일 단일 스프린트라 버퍼가 얇음 → Should/Could 를 먼저 축소해 M5 날짜를 사수한다.

## 2. 이번 주 작업 (일자 블록 · MoSCoW 우선순위)

| # | 블록 | 작업 | 등급 | 완료 기준 (게이트) |
|:-:|------|------|:---:|------|
| 0 | 월(선결) | **B-4 시드 출발일 상대날짜화** — `DataInitializer` 출발일을 `now()` 기준 상대날짜로 + `config.js` FROM_DATE·L3b 50일 매핑 동반 수정. 예매 부하/시연 경로 거짓실패 제거. **[x] 완료(07/13): 코드·빌드/테스트 + 런타임 검증(avail 50·SCARD 1000·L1 reserve_ok=1×3·오버셀 0)** | **Must(블로커)** | ✅ 재기동 후 avail 워밍업 정상(50키) + L1 `reserve_ok=1`(수동 타깃 우회 불필요) |
| 1 | 월~화 | **T6-2 아키텍처/시퀀스 다이어그램 이미지화** — 설계 문서의 흐름(입장 제어·선점 SREM/SPOP·2-tier 일관성·HELD TTL 복구)을 이미지로. README·영상 캡션이 이에 의존하므로 선행 | **Must** | 아키텍처 1장 + 핵심 시퀀스(예매 happy·동시성 경합) 이미지 확보, README 임베드 가능 |
| 2 | 화~목 | **T6-1 README 본문** — 문제정의·아키텍처(다이어그램)·기술선택 이유·**트레이드오프**·성능 그래프(E1·E2·E3 Before/After from `P4_Result.md`)·SLO 충족/미달 근거. C1·C2·C6 를 문장으로 충족 | **Must** | 7개 평가기준을 문서로 커버 + 그래프 임베드 + 트레이드오프 각 결정에 rationale |
| 3 | 목 | **T6-4 동작 영상 — 정상 흐름** — 조회→입장→좌석선택(SEAT)/자동배정(AUTO)→확정→취소 happy path 녹화 | Should | 정상 흐름 1개 영상(정상 예매·매진 표시·취소 복구 가시화) |
| 4 | 금 | **T6-5 동작 영상 — 동시성 시연 + 부하 결과** — 동시 1,000 → oversell 0 시연 + k6 부하 결과(L1/L2/E1/E2) 화면. 정합성 핵심을 눈으로 증명 | **Must** | oversell=0 시연 + 부하 그래프/수치가 영상에 포함(핵심 셀링포인트) |
| 5a | 금 | **T6-3a Swagger UI(OpenAPI) 노출** — `springdoc-openapi` 의존성 추가로 API 계약을 브라우저에서 클릭 시연 가능하게. **FE 없이도 "면접관이 직접 확인 가능한 형태"(평가기준 ④) 충족** — 조회→입장→예매(SEAT/AUTO)→확정→취소를 UI에서 호출. 배포 성공 시 공개 데모 진입점, 로컬만이면 영상 시연 진입점 | Should | `/swagger-ui.html` 노출 + 핵심 엔드포인트 그룹핑·예시 확인 |
| 5b | 금(시도) | **T6-3b 배포(URL)** — docker-compose 기반 클라우드/호스팅 시도(**백엔드 단독** + Swagger UI, FE 없음). **차단·시간초과 시 영상(T6-4/5)으로 보완하고 넘어감**(개발계획 방침) | Should | 배포 URL 확보 시 README 링크(+Swagger UI); 실패 시 사유 기록 + 영상 대체 명시 |
| 6 | 토 | **T6-6 셀프 체크리스트 7항목 점검** — `Portfolio_Project_Evaluation_Criteria.md` C1~C7 각 항목 대응 산출물 확인, 미비 항목 보강 | **Must** | C1~C7 전부 `[x]` 또는 미달 사유 명시 |
| 7 | 일 | **T6-7 최종 점검 + 제출** — README·다이어그램·영상·(배포)·체크리스트 정합성 최종 확인 → **M5 제출** | **Must** | 제출 가능 상태 확정 → **M5 완결** |

> 이번 주 완료 시 P6 = 7/7(100%), 전체 42/54(78%). P5(MQ)·P7(심화·블로그)은 M5 이후 가산 범위로 이월.

## 3. 순서·의존성 (고정)

- **B-4 → 영상/배포**: B-4(시드) 미해소 시 예매 경로 시연이 거짓 실패하므로 **영상·배포보다 반드시 선행**.
- **T6-2(다이어그램) → T6-1(README)**: README 아키텍처 절이 다이어그램 이미지에 의존.
- **T6-1·그래프 → 영상 캡션**: 영상의 부하 결과 설명이 `P4_Result.md` 수치·README 서사와 일관해야 함.
- **P4 수치는 신규 측정 아님**: E1·E2·E3 Before/After·K=100·임계점은 W6에서 확보 완료 → 이번 주는 **인용·조립**만.
- **T6-3a(Swagger) → T6-3b(배포)**: Swagger UI 가 배포 데모의 진입점이자 FE 부재의 대체 → 배포 시도 전 로컬에서 먼저 노출·검증.

## 4. 범위 밖 (이번 주 안 함)

- **P5(MQ 비동기)** T5-1~T5-3 → Could. M5 제출 상태 확보 후 버퍼 있을 때만.
- **P4 스트레치 실험** T4-10(E5 가상스레드)·T4-11(E6 락 라이브러리)·T4-12(E7 Memcached)·T4-14(전역 K) → M5 이후.
- **P7 심화 산출물** T7-1(Redis 정리)·T7-2(코레일 대비)·T7-3(블로그) → 110% 가산, 제출 후.
- **잔여 백로그** B-3(입장 슬롯 누수)는 구조적 결함이나 M5 시연 경로엔 미영향 → 제출 후 처리(발견 시 영상에서 회피).
- **FE(프론트엔드) 신규 개발** → 범위 밖. 이 프로젝트의 핵심 증거(oversell=0·Before/After·임계점)는 UI 클릭으로 재현 불가라 **k6 부하 + audit + Swagger UI**로 증명하는 게 정합적. `Won't`(다국어/모바일앱)와 같은 선상. 시각적 데모가 꼭 필요하면 **풀 FE 아닌 단일 정적 HTML(좌석 그리드 + fetch)** 로 **P7(110% 가산·M5 이후)** 이월.

## 5. 주간 DoD (Definition of Week-done)

- [x] B-4 시드 출발일 상대날짜화 — 완료(07/13, `fix/b-4-seed-relative-date`): 코드 3곳 + 런타임 검증(시드창 07/14~·avail 50·SCARD 1000·k6 FROM_DATE 일치·L1 `reserve_ok=1`×3·오버셀 0)
- [x] T6-2 아키텍처/시퀀스 다이어그램 이미지 확보 — 완료(07/14): Mermaid→SVG 3장(아키텍처·happy path·동시성 경합), 투명배경·글자색 고침, `docs/images/architecture.md` 임베드
- [x] T6-1 README 완성 — 완료(07/14): 문제정의·아키텍처(다이어그램 3장)·기술선택·트레이드오프·성능 그래프(E1·E2·E3) + C1·C2·C6 충족. 잔여 결과물=배포/영상뿐
- [ ] T6-4 동작 영상(정상 흐름) 확보
- [ ] T6-5 동작 영상(동시성 oversell=0 시연 + 부하 결과) 확보 — 핵심 셀링포인트
- [ ] T6-3a Swagger UI(`/swagger-ui.html`) 노출 — API 클릭 시연 진입점 확보(FE 대체)
- [ ] T6-3b 배포 URL 시도(백엔드 단독 + Swagger) — 확보 시 링크, 차단 시 영상 보완 + 사유 기록
- [ ] T6-6 셀프 체크리스트 C1~C7 점검 완료
- [ ] T6-7 최종 점검 + 제출 → **M5 완결(제출 가능)**
- [ ] `KTX_Ticketing_Task_Checklist.md` T6-1~T6-7 `[x]` + 진행 현황 요약표 갱신
